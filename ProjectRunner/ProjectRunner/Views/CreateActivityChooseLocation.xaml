<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProjectRunner.Views.CreateActivityChooseLocation"
             xmlns:views="clr-namespace:ProjectRunner.Views"
             Title="Create activity"
             BindingContext="{Binding CreateActivityViewModel, Source={StaticResource Locator}}">
    <ScrollView Padding="0,0,10,0" Margin="10,0,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackLayout Grid.Row="0">
                <Label Text="Choose location" FontSize="24" HorizontalOptions="Center" FontAttributes="Bold"/>

                <StackLayout Orientation="Horizontal">
                    <Picker Title="Location" ItemsSource="{Binding KnownAddress}" HorizontalOptions="FillAndExpand" SelectedIndex="{Binding SelectedIndexLocation,Mode=TwoWay}" ItemDisplayBinding="{Binding Name}"/>
                    <Button Text="X" Command="{Binding DeleteLocationCommand}" IsEnabled="{Binding SelectedIndexLocation, Converter={StaticResource IntGreaterThan}, ConverterParameter=-1}" IsVisible="{Binding SelectedIndexLocation, Converter={StaticResource IntGreaterThan}, ConverterParameter=-1}"/>
                </StackLayout>
                <Label FontSize="12" Text="La località non è presente nell'elenco? Clicca qui per aggiungerla" TextColor="Blue" HorizontalOptions="Center" Margin="0,10">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToAddLocation}"/>
                    </Label.GestureRecognizers>
                </Label>
            </StackLayout>

            <Button Grid.Row="1" Text="Next" Command="{Binding GoToConfirm}" IsEnabled="{Binding IsNextLocationEnabled}"/>
        </Grid>
    </ScrollView>
</ContentPage>